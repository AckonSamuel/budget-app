<%= form_with(model: group_expenditure) do |form| %>
  <% if group_expenditure.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(group_expenditure.errors.count, "error") %> prohibited this group_expenditure from being saved:</h2>

      <ul>
        <% group_expenditure.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
